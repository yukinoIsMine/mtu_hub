{
  "name": "mtu-hub",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev:user": "turbo run dev --filter=@mtu/user",
    "dev:admin": "turbo run dev --filter=@mtu/admin"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
